<div id="layout-header">
    <a href="/">Habit tracker</a>

    <% if(locals.user) { %>
        <div>
            <span> <% user.name %> </span>

            <!-- TODO:  change the signout link-->
            <a href="/users/logout"> logout </a>
        </div>
    <% } %>
</div>